var inicio = parseInt(prompt("Digite o número de início."));
var final = parseInt(prompt("Digite o número de fim."));

for (var index = inicio; index <= final ; index++) {
    console.log(index);   
}

